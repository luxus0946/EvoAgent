version: "3.9"

services:
  api:
    build: .
    command: uvicorn app.api:app --host 0.0.0.0 --port 8000
    ports:
      - "8000:8000"

  ui:
    build: .
    command: python app/gradio_app.py
    ports:
      - "7860:7860"
    depends_on:
      - api